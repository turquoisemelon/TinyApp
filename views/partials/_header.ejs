<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% if(username){ %>
      <p>Logged in as <%= username %><p>
        <form class="uselater" action="/logout" method="POST">
          <button type="submit">Logout</button>
        </form>
    <% } else{ %>
      <form class="uselater" action="/login" method="POST">
        <input id="id" type="text" name="username" style="width: 300px">
        <button type="submit">Log in</button>
      </form>
    <% } %>
  </body>
</html>
